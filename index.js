let lista = [12, 5, 6, 55, 0.5, 63, 22, 11, 3];

function multiplicarNumeros(base, multiplicador, lista){
    const validarYMultiplicar = (lista).forEach(element => {
   
    });
    return console.log(lista);
};

multiplicarNumeros(20, 2, lista);